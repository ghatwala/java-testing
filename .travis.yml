matrix:
  include:
    - env: ENV=bionic
      dist: bionic
      arch: ppc64le
    - env: ENV=xenial
      dist: xenial
      arch: ppc64le
language: java
jdk:
  - openjdk8
  - openjdk11
  - openjdk12
  - openjdk13
dist: 
  - xenial
  - bionic
script:
  - java -version
  - echo ${TRAVIS_CPU_ARCH}
